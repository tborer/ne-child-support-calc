{
  "name": "ne-child-support-calc",
  "version": "1.0.0",
  "description": "Nebraska Child Support Calculator — automated E2E tests",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
